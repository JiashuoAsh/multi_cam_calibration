你是资深 Python/CV 工程师，请在当前仓库里对 Apriltag 检测流程做“性能优先”的重构与优化，目标是把 400 张图片的 Apriltag 检测从“数小时级”降到“分钟级/可接受范围”，同时尽量不牺牲标定成功率与稳定性。

背景/问题：
- 目前 Apriltag 检测非常耗时；代码里很多地方会把“所有图片都检测一遍”。
- 我有 ~400 张图片，现状可能仅检测就要 3 小时。
- 例如 step4 里有 MAX_VALID_IMAGES 之类的参数，但整体设计仍倾向于全量检测，导致不必要的开销。
- 我希望：能显著提速，并且逻辑更清晰（高内聚低耦合），模块可独立运行/测试。可以大胆重构文件结构（不要改 msv 目录）。

你需要完成的改动（按优先级）：
1) “早停/上限”策略：将 step4（以及任何类似阶段）从“先全量检测再筛选”改成“边扫描边检测边决策”：
   - 一旦收集到足够的 valid images（达到 MAX_VALID_IMAGES 或配置的目标数量）就立刻停止后续图片检测。
   - 支持额外的上限：MAX_TOTAL_IMAGES（最多扫描/尝试多少张），MAX_DETECT_SECONDS（总检测时间上限），超过即停止并给出可读的告警/统计。
   - 选择策略可配置：顺序/随机（可设 seed 保证可复现）/均匀采样（例如按时间戳/序号分桶抽样）。

2) 缓存（强制要求）：避免重复检测同一张图
   - 设计一个 detection cache（建议 cache_dir 下每张图一个 json/npz，或一个聚合文件），key 包含：图片路径 + 文件mtime/size + detector参数（family, quad_decimate, quad_sigma 等）。
   - 默认启用缓存：已检测过的图片直接读缓存结果，不再跑 detector。
   - 提供 --force-redetect 或配置项用于忽略缓存。

3) 并行化（在不破坏稳定性的前提下）：
   - 若 apriltag 检测是 CPU 密集：优先用 multiprocessing（ProcessPoolExecutor / multiprocessing.Pool），避免 GIL 影响。
   - 允许配置 workers 数（默认=CPU核数或合理值），并确保在 Windows/Linux 下都能跑。
   - 注意：detector 实例是否可序列化/线程安全；如果不安全，在 worker 内初始化 detector。

4) “廉价预筛选”可选但建议（显著减少 detector 调用次数）：
   - 在进入 Apriltag detector 前先做快速判别（O(N)且很便宜），例如：
     - 图像亮度/对比度阈值、模糊度（Laplacian var）、过曝/欠曝比例、边缘密度等；
   - 通过预筛选的再进入 detector；预筛选阈值可配置，并在日志里输出被过滤的比例。

5) 可观测性与验收：
   - 增加统计输出：总图片数、实际检测数、命中缓存数、预筛选过滤数、平均/95p单张检测耗时、总耗时。
   - 给一个简单的 bench 脚本或命令，能对同一数据集跑 before/after（至少输出 timing）。
   - 保持/补充必要的单元测试：重点测“早停是否生效”“缓存命中逻辑正确”“随机/采样策略可复现”。

工程约束：
- 允许重构目录与模块拆分，但不要改 msv 目录。
- 模块要能独立运行与测试；高内聚低耦合。
- 不要保留旧实现的兼容层（直接替换成更清晰的新设计）。
- 注释必须用中文，重点解释非直观的策略/边界条件。
- 如果有 CLI/配置文件，参数命名要直观，且在 README/使用说明里补齐。

交付结果：
- 完成代码修改：把全量检测改成“流式+早停+缓存+并行（+可选预筛选）”。
- 更新/新增入口或配置，使我能一条命令跑 step4 并看到提速效果与统计输出。
- 提供最小可运行示例（即使没有真实相机参数/真实数据，也能用你自造的小样例跑通流程）。

现在开始：请先定位仓库里所有调用 apriltag detector 的位置，梳理调用链，给出重构方案并直接落地到代码里（包含 tests/README/bench）。
